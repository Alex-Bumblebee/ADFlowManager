<Page
    x:Class="ADFlowManager.UI.Views.Pages.ComputersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:converters="clr-namespace:ADFlowManager.UI.Converters"
    xmlns:local="clr-namespace:ADFlowManager.UI.Views.Pages"
    xmlns:ext="clr-namespace:ADFlowManager.UI.Extensions"
    Title="Ordinateurs"
    d:DesignHeight="800"
    d:DesignWidth="1200"
    ScrollViewer.VerticalScrollBarVisibility="Disabled"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:BoolToStatusColorConverter x:Key="BoolToStatusColorConverter" />
        <converters:BoolToStatusTextConverter x:Key="BoolToStatusTextConverter" />
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        <converters:InverseNullToVisibilityConverter x:Key="InverseNullToVisibilityConverter" />
    </Page.Resources>

    <Grid Margin="24,8,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- === TOOLBAR === -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Search box -->
            <ui:TextBox
                Grid.Column="0"
                PlaceholderText="{ext:Localized Computers_SearchPlaceholder}"
                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                Background="#2D2D30"
                Foreground="#F5F5F5"
                BorderBrush="#3F3F46"
                Height="44"
                FontSize="14"
                Margin="0,0,16,0">
                <ui:TextBox.Icon>
                    <ui:SymbolIcon Symbol="Search24" Foreground="#8B5CF6" />
                </ui:TextBox.Icon>
            </ui:TextBox>

            <!-- Computer count badge -->
            <Border
                Grid.Column="1"
                Background="#8B5CF6"
                CornerRadius="20"
                Padding="16,8"
                VerticalAlignment="Center"
                Margin="0,0,16,0">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon
                        Symbol="Desktop24"
                        FontSize="18"
                        Foreground="White"
                        Margin="0,0,8,0" />
                    <TextBlock
                        FontSize="14"
                        FontWeight="SemiBold"
                        Foreground="White">
                        <Run Text="{Binding ComputerCount, Mode=OneWay}" />
                        <Run Text="{ext:Localized Computers_ComputerCount}" />
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Actions toolbar -->
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <ui:Button
                    Content="{ext:Localized Computers_CheckStatuses}"
                    Appearance="Secondary"
                    Command="{Binding CheckAllOnlineStatusCommand}"
                    Height="44"
                    Margin="0,0,8,0"
                    Padding="16,0">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="Wifi124" />
                    </ui:Button.Icon>
                </ui:Button>
                <ui:Button
                    Appearance="Secondary"
                    Command="{Binding LoadComputersCommand}"
                    ToolTip="{ext:Localized Computers_RefreshTooltip}"
                    Height="44"
                    Width="44">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="ArrowSync24" />
                    </ui:Button.Icon>
                </ui:Button>
            </StackPanel>
        </Grid>

        <!-- === CONTENT: DataGrid + Details (same layout as UsersPage / GroupsPage) === -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="7*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <!-- === LEFT: DataGrid === -->
            <Border
                Grid.Column="0"
                Background="#1E1E1E"
                BorderBrush="#3F3F46"
                BorderThickness="1"
                CornerRadius="8"
                ClipToBounds="True"
                MaxHeight="800"
                VerticalAlignment="Top">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <DataGrid
                        Grid.Row="0"
                        x:Name="ComputersDataGrid"
                        ItemsSource="{Binding Computers}"
                        SelectedItem="{Binding SelectedComputer, Mode=TwoWay}"
                        AutoGenerateColumns="False"
                        IsReadOnly="True"
                        SelectionMode="Single"
                        CanUserSortColumns="True"
                        CanUserResizeColumns="True"
                        CanUserReorderColumns="False"
                        HeadersVisibility="Column"
                        GridLinesVisibility="None"
                        Background="Transparent"
                        Foreground="#F5F5F5"
                        BorderThickness="0"
                        RowHeaderWidth="0"
                        FontSize="13"
                        MouseDoubleClick="DataGrid_MouseDoubleClick"
                        ScrollViewer.CanContentScroll="True"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        VirtualizingPanel.IsVirtualizing="True"
                        VirtualizingPanel.VirtualizationMode="Recycling"
                        EnableRowVirtualization="True">

                        <DataGrid.Resources>
                            <!-- Row style -->
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderBrush" Value="#2D2D30" />
                                <Setter Property="BorderThickness" Value="0,0,0,1" />
                                <Setter Property="MinHeight" Value="48" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#2D2D30" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#3B2D5E" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>

                            <!-- Cell style -->
                            <Style TargetType="DataGridCell">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Foreground" Value="#F5F5F5" />
                                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border
                                                Background="Transparent"
                                                Padding="12,8">
                                                <ContentPresenter VerticalAlignment="Center" />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="Transparent" />
                                        <Setter Property="Foreground" Value="#F5F5F5" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>

                            <!-- Column header style -->
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#161616" />
                                <Setter Property="Foreground" Value="#A1A1AA" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="FontSize" Value="12" />
                                <Setter Property="Padding" Value="12,10" />
                                <Setter Property="BorderBrush" Value="#2D2D30" />
                                <Setter Property="BorderThickness" Value="0,0,0,1" />
                                <Setter Property="HorizontalContentAlignment" Value="Left" />
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <!-- Avatar + Nom ordinateur -->
                            <DataGridTemplateColumn Header="{ext:Localized Computers_ColName}" Width="2*" SortMemberPath="Computer.Name">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border
                                                Width="32" Height="32"
                                                CornerRadius="6"
                                                Background="#3B2D5E"
                                                Margin="0,0,10,0">
                                                <ui:SymbolIcon
                                                    Symbol="Desktop24"
                                                    FontSize="16"
                                                    Foreground="#8B5CF6"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center" />
                                            </Border>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock
                                                    Text="{Binding Computer.Name}"
                                                    FontWeight="SemiBold"
                                                    Foreground="#F5F5F5"
                                                    FontSize="13" />
                                                <TextBlock
                                                    Text="{Binding Computer.OperatingSystem}"
                                                    Foreground="#71717A"
                                                    FontSize="11" />
                                            </StackPanel>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Version OS -->
                            <DataGridTemplateColumn Header="{ext:Localized Computers_ColVersion}" Width="120" SortMemberPath="Computer.OperatingSystemVersion">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Text="{Binding Computer.OperatingSystemVersion}"
                                            Foreground="#A1A1AA"
                                            FontSize="13"
                                            VerticalAlignment="Center"
                                            TextTrimming="CharacterEllipsis" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Dernière connexion -->
                            <DataGridTemplateColumn Header="{ext:Localized Computers_ColLastLogon}" Width="160" SortMemberPath="Computer.LastLogon">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Text="{Binding Computer.LastLogon, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}"
                                            Foreground="#A1A1AA"
                                            FontSize="13"
                                            VerticalAlignment="Center" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Statut -->
                            <DataGridTemplateColumn Header="{ext:Localized Computers_ColStatus}" Width="120" SortMemberPath="Computer.Enabled">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border
                                            CornerRadius="4"
                                            Padding="8,4"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#1A10B981" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Computer.Enabled}" Value="False">
                                                            <Setter Property="Background" Value="#1AEF4444" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock
                                                Text="{Binding Computer.Enabled, Converter={StaticResource BoolToStatusTextConverter}}"
                                                FontSize="12"
                                                FontWeight="SemiBold">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#10B981" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Computer.Enabled}" Value="False">
                                                                <Setter Property="Foreground" Value="#EF4444" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Pagination footer -->
                    <Border
                        Grid.Row="1"
                        Background="#252525"
                        Padding="16,10"
                        BorderBrush="#3F3F46"
                        BorderThickness="0,1,0,0">
                        <TextBlock
                            FontSize="12"
                            Foreground="#71717A"
                            HorizontalAlignment="Center">
                            <Run Text="{ext:Localized Computers_ShowingOf}"/>
                            <Run Text="{Binding Computers.Count, Mode=OneWay}" FontWeight="SemiBold" Foreground="#8B5CF6"/>
                            <Run Text="{ext:Localized Computers_ComputerWord}"/>
                        </TextBlock>
                    </Border>
                </Grid>
            </Border>

            <!-- === RIGHT: Details Panel === -->
            <!-- Placeholder quand aucun ordinateur sélectionné -->
            <Border
                Grid.Column="2"
                Background="#1E1E1E"
                BorderBrush="#3F3F46"
                BorderThickness="1"
                CornerRadius="8"
                Padding="24"
                MaxHeight="800"
                VerticalAlignment="Top"
                Visibility="{Binding SelectedComputer, Converter={StaticResource InverseNullToVisibilityConverter}}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ui:SymbolIcon
                        Symbol="Desktop24"
                        FontSize="48"
                        Foreground="#3F3F46"
                        HorizontalAlignment="Center" />
                    <TextBlock
                        Text="{ext:Localized Computers_SelectComputer}"
                        FontSize="16"
                        Foreground="#71717A"
                        HorizontalAlignment="Center"
                        Margin="0,16,0,4" />
                    <TextBlock
                        Text="{ext:Localized Computers_ToViewDetails}"
                        FontSize="14"
                        Foreground="#52525B"
                        HorizontalAlignment="Center" />
                </StackPanel>
            </Border>

            <!-- Détails ordinateur sélectionné -->
            <Border
                Grid.Column="2"
                Background="#1E1E1E"
                BorderBrush="#3F3F46"
                BorderThickness="1"
                CornerRadius="8"
                MaxHeight="800"
                VerticalAlignment="Top"
                Visibility="{Binding SelectedComputer, Converter={StaticResource NullToVisibilityConverter}}">

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="24">
                        <!-- Avatar + Nom -->
                        <StackPanel HorizontalAlignment="Center" Margin="0,0,0,24">
                            <Border
                                Width="72" Height="72"
                                CornerRadius="8"
                                Background="#3B2D5E"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,12">
                                <ui:SymbolIcon
                                    Symbol="Desktop24"
                                    FontSize="32"
                                    Foreground="#8B5CF6"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </Border>
                            <TextBlock
                                Text="{Binding SelectedComputer.Computer.Name}"
                                FontSize="20"
                                FontWeight="Bold"
                                Foreground="#F5F5F5"
                                HorizontalAlignment="Center" />
                            <TextBlock
                                Text="{Binding SelectedComputer.Computer.OperatingSystem}"
                                FontSize="14"
                                Foreground="#A1A1AA"
                                HorizontalAlignment="Center"
                                Margin="0,4,0,8" />

                            <!-- Badge statut -->
                            <Border
                                HorizontalAlignment="Center"
                                CornerRadius="12"
                                Padding="12,4">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedComputer.Computer.Enabled}" Value="False">
                                                <Setter Property="Background" Value="#1AEF4444" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock
                                    Text="{Binding SelectedComputer.Computer.Enabled, Converter={StaticResource BoolToStatusTextConverter}}"
                                    FontSize="12"
                                    FontWeight="SemiBold"
                                    HorizontalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#10B981" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedComputer.Computer.Enabled}" Value="False">
                                                    <Setter Property="Foreground" Value="#EF4444" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </StackPanel>

                        <!-- Séparateur -->
                        <Border Height="1" Background="#3F3F46" Margin="0,0,0,20" />

                        <!-- Informations -->
                        <TextBlock
                            Text="{ext:Localized Computers_Information}"
                            FontSize="11"
                            FontWeight="Bold"
                            Foreground="#71717A"
                            Margin="0,0,0,12" />

                        <!-- Version OS -->
                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="{ext:Localized Computers_VersionOS}" FontSize="11" Foreground="#71717A" />
                            <TextBlock
                                Text="{Binding SelectedComputer.Computer.OperatingSystemVersion}"
                                FontSize="14" Foreground="#F5F5F5"
                                Margin="0,2,0,0" />
                        </StackPanel>

                        <!-- Description -->
                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="{ext:Localized Computers_Description}" FontSize="11" Foreground="#71717A" />
                            <TextBlock
                                Text="{Binding SelectedComputer.Computer.Description}"
                                FontSize="14" Foreground="#F5F5F5"
                                TextWrapping="Wrap"
                                Margin="0,2,0,0" />
                        </StackPanel>

                        <!-- Emplacement -->
                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="{ext:Localized Computers_Location}" FontSize="11" Foreground="#71717A" />
                            <TextBlock
                                Text="{Binding SelectedComputer.Computer.Location}"
                                FontSize="14" Foreground="#F5F5F5"
                                Margin="0,2,0,0" />
                        </StackPanel>

                        <!-- Dernière connexion -->
                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="{ext:Localized Computers_LastLogon}" FontSize="11" Foreground="#71717A" />
                            <TextBlock
                                Text="{Binding SelectedComputer.Computer.LastLogon, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}"
                                FontSize="14" Foreground="#F5F5F5"
                                Margin="0,2,0,0" />
                        </StackPanel>

                        <!-- DN -->
                        <StackPanel Margin="0,0,0,20">
                            <TextBlock Text="{ext:Localized Computers_DN}" FontSize="11" Foreground="#71717A" />
                            <TextBlock
                                Text="{Binding SelectedComputer.Computer.DistinguishedName}"
                                FontSize="12" Foreground="#A1A1AA"
                                TextWrapping="Wrap"
                                Margin="0,2,0,0" />
                        </StackPanel>

                        <!-- Séparateur -->
                        <Border Height="1" Background="#3F3F46" Margin="0,0,0,20" />

                        <!-- Dates -->
                        <TextBlock
                            Text="{ext:Localized Computers_Dates}"
                            FontSize="11"
                            FontWeight="Bold"
                            Foreground="#71717A"
                            Margin="0,0,0,12" />

                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="{ext:Localized Computers_CreatedAt}" FontSize="11" Foreground="#71717A" />
                            <TextBlock
                                Text="{Binding SelectedComputer.Computer.Created, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}"
                                FontSize="14" Foreground="#F5F5F5"
                                Margin="0,2,0,0" />
                        </StackPanel>

                        <StackPanel Margin="0,0,0,20">
                            <TextBlock Text="{ext:Localized Computers_ModifiedAt}" FontSize="11" Foreground="#71717A" />
                            <TextBlock
                                Text="{Binding SelectedComputer.Computer.Modified, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}"
                                FontSize="14" Foreground="#F5F5F5"
                                Margin="0,2,0,0" />
                        </StackPanel>

                        <!-- Séparateur -->
                        <Border Height="1" Background="#3F3F46" Margin="0,0,0,20" />

                        <!-- Actions -->
                        <TextBlock
                            Text="{ext:Localized Computers_Actions}"
                            FontSize="11"
                            FontWeight="Bold"
                            Foreground="#71717A"
                            Margin="0,0,0,12" />

                        <ui:Button
                            Content="{ext:Localized Computers_CheckStatus}"
                            Appearance="Primary"
                            Command="{Binding CheckOnlineStatusCommand}"
                            HorizontalAlignment="Stretch"
                            Height="40"
                            Margin="0,0,0,8">
                            <ui:Button.Icon>
                                <ui:SymbolIcon Symbol="Wifi124" />
                            </ui:Button.Icon>
                        </ui:Button>

                        <ui:Button
                            Content="{ext:Localized Computers_OpenDetails}"
                            Appearance="Secondary"
                            Click="OpenDetailsButton_Click"
                            HorizontalAlignment="Stretch"
                            Height="40">
                            <ui:Button.Icon>
                                <ui:SymbolIcon Symbol="Open24" />
                            </ui:Button.Icon>
                        </ui:Button>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Loader overlay -->
        <Border
            Grid.Row="0"
            Grid.RowSpan="2"
            Background="#CC0F0F0F"
            Panel.ZIndex="999"
            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <ui:ProgressRing
                    IsIndeterminate="True"
                    Width="48"
                    Height="48"
                    Foreground="#8B5CF6"
                    Margin="0,0,0,16" />
                <TextBlock
                    Text="{ext:Localized Common_Loading}"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Foreground="#F5F5F5"
                    HorizontalAlignment="Center" />
                <TextBlock
                    Text="{Binding LoadingText}"
                    FontSize="13"
                    Foreground="#A0A0A0"
                    HorizontalAlignment="Center"
                    Margin="0,6,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
